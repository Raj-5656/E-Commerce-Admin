import React from 'react'
import type { CommonTableProps } from '../../types/CommomType'

const CommonTable = ({ table }: CommonTableProps) => {
  return (
    <div>CommonTable</div>
  )
}

export default CommonTable